{
  "name": "pitcher-app-tutorials",
  "version": "1.0.0",
  "scripts": {
    "publish": "pit app publish",
    "publish:url": "pit app publish --api-url ENTER_PUBLISH_API_URL"
  },
  "devDependencies": {
    "@pitcher/cli-scripts": "^1.0.0"
  }
}
